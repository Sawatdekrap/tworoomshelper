import styled from 'styled-components';

import IMG_PLAYER from 'assets/icons/players.jpg';

const Icon = styled.img`
    display: inline-block;
`;

export const ICON_PLAYER = <Icon src={IMG_PLAYER} />;
